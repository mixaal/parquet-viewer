# parquet-viewer

## Overview

Can view parquet files on object-store, including those in zip files. This makes
easier navigation in real world datasets, e.g. from kaggle.

## Usage
```
cd pblaha-parquet-testing.zip
ls
view parquet-testing/data/int32_decimal.parquet
```
